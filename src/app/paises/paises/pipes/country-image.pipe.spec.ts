import { CountryImagePipe } from './country-image.pipe';

describe('CountryImagePipe', () => {
  it('create an instance', () => {
    const pipe = new CountryImagePipe();
    expect(pipe).toBeTruthy();
  });
});
